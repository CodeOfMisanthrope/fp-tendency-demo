export {
   map,
   filter,
   enumerate,
   take,
   repeat,
   count,
   first,
   last,
   toArray,
   toSet,
   slice,
} from "~core/iter";

export { curry } from "~core/utils";

export { Result } from "~core/containers";
